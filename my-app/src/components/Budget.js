import React, { useState } from "react";
import { Row, Col } from "react-bootstrap";
import "./Budget.css";
const Budget = () => {
  return (
    <div>
      {/* {ShowBudget.map((val, i) => {
        <Row className=" main-row ">
          <Col className="budgetSmall">{val.budget}</Col>
          <Col className="price">{val.price}</Col>
          <Col className="discount">{val.discount}</Col>
        </Row>;
      })} */}
    </div>
  );
};
export default Budget;
